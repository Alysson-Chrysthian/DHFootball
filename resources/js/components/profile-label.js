const profileImageContainer = document.querySelector('.profile-label-container');
const fileInput = document.querySelector('.file-input');

profileImageContainer.addEventListener('click', () => {    
    fileInput.click();
});
